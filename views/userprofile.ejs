<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="../public/css/style.css">
    <style>
        body {
            font-family: 'Arial, sans-serif';
            background-color: #f8f9fa;
        }

        .vh-100 {
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .card {
            border: none;
            border-radius: 1rem;
            box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.1);
        }

        .card-body {
            padding: 5rem;
        }

        .card-title {
            font-size: 2rem;
            font-weight: bold;
            margin-bottom: 1.5rem;
            color: #343a40;
        }

        .card-subtitle {
            font-size: 1.2rem;
            margin-bottom: 3rem;
            color: #6c757d;
        }

        .table th, .table td {
            border-top: none;
        }

        .btn-edit, .btn-logout {
            width: 150px;
        }

        .btn-edit {
            background-color: #007bff;
            color: #fff;
        }

        .btn-logout {
            background-color: #dc3545;
            color: #fff;
        }
    </style>
</head>
<body>
    <section class="vh-100">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-lg-8 col-xl-6">
                    <div class="card">
                        <div class="card-body text-center">
                            <h2 class="card-title fw-bold text-uppercase">Welcome to Your Daashboard</h2>

                            <% if (locals.name) { %>
                                <p class="card-subtitle"><%= locals.name %></p>
                            <% } else { %>
                                <p class="card-subtitle">Welcome, User!</p>
                            <% } %>

                            <table class="table table-hover">
                                <tr>
                                    <td>Name:</td>
                                    <% if(locals.user) { %>
                                        <td><%= locals.user.name %></td>
                                    <% } %>
                                </tr>
                                <tr>
                                    <td>E-mail:</td>
                                    <% if(locals.user) { %>
                                        <td><%= locals.user.email %></td>
                                    <% } %>
                                </tr>
                                <tr>
                                    <td>Mobile:</td>
                                    <% if(locals.user) { %>
                                        <td><%= locals.user.mobile %></td>
                                    <% } %>
                                </tr>
                                <tr>
                                    <td>Password:</td>
                                    <% if(locals.user) { %>
                                        <% var len = locals.user.password.length %>
                                        <td><%= '*'.repeat(len) %></td>
                                    <% } %>
                                </tr>
                            </table>

                            <a href="/userhome/editprofile" class="btn btn-edit">Edit Profile</a>
                            <a href="/logout" class="btn btn-logout">Logout</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js" integrity="sha384-IQsoLXl5PILFhosVNubq5LC7Qb9DXgDA9i+tQ8Zj3iwWAwPtgFTxbJ8NT4GN1R8p" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.min.js" integrity="sha384-cVKIPhGWiC2Al4u+LWgxfKTRIcfu0JTxR+EQDz/bgldoEyl4H0zUF0QKbrJ0EcQF" crossorigin="anonymous"></script>
</body>
</html>
