<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Profile</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="../public/css/style.css">
    <style>
        body {
            font-family: 'Arial, sans-serif';
            background-color: #f8f9fa;
        }

        .vh-100 {
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .card {
            border: none;
            border-radius: 1rem;
            box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.1);
            background: rgba(255, 255, 255, 0.9);
            width: 80%;
            max-width: 600px;
            overflow: hidden;
            position: relative;
        }

        .card-body {
            padding: 5rem;
        }

        .card-title {
            font-size: 2rem;
            font-weight: bold;
            margin-bottom: 1.5rem;
            color: #007bff; /* Blue color for the title */
        }

        .card-subtitle {
            font-size: 1.2rem;
            margin-bottom: 3rem;
            color: #6c757d;
        }

        .form-control {
            background-color: #f8f9fa; /* Light gray background */
            border-color: #007bff; /* Blue border color */
            color: #000; /* Black text color */
            border-radius: 0;
        }

        .btn-edit, .btn-logout {
            width: 150px;
            border-radius: 0;
            margin-top: 10px;
            transition: 0.3s;
        }

        .btn-edit {
            background-color: #007bff;
            color: #fff;
        }

        .btn-edit:hover {
            background-color: #0056b3; /* Darker blue on hover */
        }

        .btn-logout {
            background-color: #dc3545;
            color: #fff;
        }

        .btn-logout:hover {
            background-color: #bd2130; /* Darker red on hover */
        }

        .wave {
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            height: auto;
            z-index: -1;
        }
    </style>
</head>
<body>
    <section class="vh-100">
        <div class="card">
            <div class="card-body text-center">
                <h2 class="card-title fw-bold text-uppercase">Welcome to Your Dashboard</h2>

                <% if (locals.name) { %>
                    <p class="card-subtitle"><%= locals.name %></p>
                <% } else { %>
                    <p class="card-subtitle">Welcome, User!</p>
                <% } %>

                <% if(locals.err) { %>
                    <div class="alert alert-danger"> <%= locals.err %></div>
                <% } %>

                <form action="/userhome/editprofile" method="post">
                    <div class="form-group">
                        <label for="inputName">Name:</label>
                        <input type="text" class="form-control" id="inputName" name="name" value="<%= locals.user.name %>" required />
                    </div>
                    <div class="form-group">
                        <label for="inputEmail">E-mail:</label>
                        <input type="email" class="form-control" id="inputEmail" name="email" value="<%= locals.user.email %>" required />
                    </div>
                    <div class="form-group">
                        <label for="inputMobile">Mobile:</label>
                        <input type="number" class="form-control" id="inputMobile" name="mob" value="<%= locals.user.mobile %>" required />
                    </div>
                    <div class="form-group">
                        <label for="inputCurrentPassword">Current Password:</label>
                        <input type="password" class="form-control" id="inputCurrentPassword" name="pass1" required />
                    </div>
                    <div class="form-group">
                        <label for="inputNewPassword">New Password:</label>
                        <input type="password" class="form-control" id="inputNewPassword" name="pass2" required />
                    </div>

                    <button type="submit" class="btn btn-edit">Edit</button>
                </form>

                <a href="/logout" class="btn btn-logout">Logout</a>
            </div>
        </div>
        <img src="https://www.transparenttextures.com/patterns/dark-matter.png" alt="wave" class="wave">
    </section>

    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js" integrity="sha384-IQsoLXl5PILFhosVNubq5LC7Qb9DXgDA9i+tQ8Zj3iwWAwPtgFTxbJ8NT4GN1R8p" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.min.js" integrity="sha384-cVKIPhGWiC2Al4u+LWgxfKTRIcfu0JTxR+EQDz/bgldoEyl4H0zUF0QKbrJ0EcQF" crossorigin="anonymous"></script>
</body>
</html>
